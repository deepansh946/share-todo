import styled from "styled-components";

export const Input = styled.input`
  border: none;
  border-bottom: 3px solid grey;
  padding: 0.4rem;
  font-size: 1.2em;
  width: 100%;

  &:focus {
    outline: none;
  }
`;
